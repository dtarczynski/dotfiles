# Install Starship into the Powershell

## 1. Create your Starship configuration file, from GitBash: 

```
mkdir -p ~/.config && touch ~/.config/starship.toml
```

## 2. Apply the config

```
mv ./starship.toml ~/.config/starship.toml
```